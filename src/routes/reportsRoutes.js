const router = require("express").Router();
const reports = require("../controllers/reportsController");
// const { isAdmin } = require("../middlewares/roleMiddleware");

// router.post("/machine-wise",  reports.machineWiseDispense);
// router.post("/state-district",  reports.stateDistrictComparison);
// router.get("/machine-refill",  reports.machineDispenseRefill);
// router.post("/summary",  reports.reportSummary);

module.exports = router;